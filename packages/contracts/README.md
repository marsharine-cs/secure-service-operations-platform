# Shared contracts

**Status:** Planned.

This directory will contain source schemas, generated API types, or clients shared across repository components. Business logic must remain in the owning application rather than being hidden in a generic shared package.
