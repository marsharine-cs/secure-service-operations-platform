# AI triage service boundary

**Status:** Planned; no service code has been implemented.

This directory will contain a Python FastAPI service for structured case-triage suggestions. It will validate inputs and outputs, record safe evaluation traces, and support deterministic test substitutes.

The service has no authority to modify operational records. Human approval is required through the primary API.
